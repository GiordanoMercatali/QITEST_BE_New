# QI TEST
## JDBC connection
Nel package `com.advancia.qitest.services.GeCV` si trova la classe `GeCVDbConnection` che gestisce la connessione al database. In questo file bisogna modificare `url`, `username` e `password` a seconda della macchina su cui è installata l'applicazione.